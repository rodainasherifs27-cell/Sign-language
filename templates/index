<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Language Detection</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <h1><i class="fas fa-sign-language"></i> Sign Language Detection</h1>
            <p class="subtitle">Upload an image or use webcam to detect ASL alphabet signs</p>
        </div>
    </header>

    <main class="container">
        <!-- Two-column layout -->
        <div class="main-content">
            <!-- Left Column: Upload/Webcam -->
            <div class="input-section">
                <!-- Upload Card -->
                <div class="card">
                    <h2><i class="fas fa-upload"></i> Upload Image</h2>
                    <form id="uploadForm" action="/predict" method="POST" enctype="multipart/form-data">
                        <div class="upload-area" id="dropArea">
                            <i class="fas fa-cloud-upload-alt fa-3x"></i>
                            <p>Drag & drop an image here</p>
                            <p class="small">or</p>
                            <label for="fileInput" class="btn">
                                <i class="fas fa-folder-open"></i> Choose File
                            </label>
                            <input type="file" id="fileInput" name="file" accept="image/*" required hidden>
                            <p class="small">Supports: JPG, PNG, JPEG (Max 5MB)</p>
                        </div>
                        <div id="fileName" class="file-name"></div>
                        <button type="submit" class="btn detect-btn" id="detectBtn">
                            <i class="fas fa-search"></i> Detect Sign
                        </button>
                    </form>
                </div>

                <!-- Webcam Card -->
                <div class="card">
                    <h2><i class="fas fa-camera"></i> Use Webcam</h2>
                    <div class="webcam-container">
                        <video id="webcam" width="100%" autoplay></video>
                        <div class="webcam-controls">
                            <button type="button" class="btn" id="startWebcam">
                                <i class="fas fa-play"></i> Start Camera
                            </button>
                            <button type="button" class="btn" id="captureBtn" disabled>
                                <i class="fas fa-camera"></i> Capture & Detect
                            </button>
                            <button type="button" class="btn" id="stopWebcam" disabled>
                                <i class="fas fa-stop"></i> Stop
                            </button>
                        </div>
                        <canvas id="canvas" width="640" height="480" hidden></canvas>
                    </div>
                </div>
            </div>

            <!-- Right Column: Results -->
            <div class="result-section">
                <div class="card result-card">
                    <h2><i class="fas fa-chart-bar"></i> Detection Result</h2>
                    
                    <!-- Loading Spinner -->
                    <div id="loading" class="loading" style="display: none;">
                        <div class="spinner"></div>
                        <p>Analyzing image...</p>
                    </div>
                    
                    <!-- Results Display -->
                    <div id="resultDisplay">
                        <div class="placeholder-result">
                            <div class="placeholder-icon">
                                <i class="fas fa-hand-paper fa-4x"></i>
                            </div>
                            <p class="placeholder-text">
                                Upload an image or capture from webcam to see the detection result.
                            </p>
                        </div>
                        
                        <!-- This will be filled by JavaScript -->
                        <div id="predictionResult" style="display: none;">
                            <div class="prediction-header">
                                <h3>Detected Sign: <span id="predictedLetter" class="letter-badge">A</span></h3>
                                <div class="confidence">
                                    Confidence: <span id="confidenceValue">95%</span>
                                </div>
                            </div>
                            
                            <!-- Preview Image -->
                            <div class="image-preview">
                                <img id="resultImage" src="" alt="Processed Image">
                            </div>
                            
                            <!-- Letter Info -->
                            <div class="letter-info">
                                <h4><i class="fas fa-info-circle"></i> About this sign:</h4>
                                <p id="letterDescription">Description will appear here...</p>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Sign Language Chart -->
                <div class="card">
                    <h2><i class="fas fa-language"></i> ASL Alphabet Reference</h2>
                    <div class="alphabet-grid" id="alphabetGrid">
                        <!-- Will be populated by JavaScript -->
                        <p>Loading alphabet chart...</p>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>Sign Language Detection System &copy; 2025 | Powered by Flask & TensorFlow</p>
            <p class="footer-links">
                <a href="#"><i class="fas fa-info-circle"></i> About</a> |
                <a href="#"><i class="fas fa-envelope"></i> Contact</a> |
                <a href="#"><i class="fab fa-github"></i> GitHub</a>
            </p>
        </div>
    </footer>

    <!-- JavaScript -->
    <script src="{{ url_for('static', filename='script.js') }}"></script>
</body>
</html>